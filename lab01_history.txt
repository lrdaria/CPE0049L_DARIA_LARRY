    1  git config --global user.name "Larry Dean R. Daria"
    2  git config --global user.email "lrdaria@feualabang.edu.ph"
    3  git config --list
    4  # Create the virtual environment folder named '.venv'
    5  python3 -m venv .venv
    6  # Activate the environment
    7  source .venv/bin/activate
    8  # Create a file to hide the virtual environment from version control
    9  echo ".venv/" > .gitignore
   10  echo "__pycache__/" >> .gitignore
   11  source /workspaces/CPE0049L_DARIA_LARRY/.venv/bin/activate
   12  # Create the virtual environment folder named '.venv'
   13  python3 -m venv .venv
   14  # Activate the environment
   15  source .venv/bin/activate
   16  # Create a file to hide the virtual environment from version control
   17  echo ".venv/" > .gitignore
   18  echo "__pycache__/" >> .gitignore
   19  # Stage and commit the scaffolding
   20  git add .gitignore
   21  git commit -m "chore: initial project scaffolding and gitignore"
   22  # Push the commit to the main branch
   23  git push origin main
   24  # Export your command history to a text file
   25  history > lab01_history.txt
On branch main
Your branch is up to date with 'origin/main'.

Untracked files:
  (use "git add <file>..." to include in what will be committed)
	lab01_history.txt

nothing added to commit but untracked files present (use "git add" to track)
